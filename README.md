# 殷嘉铖 - 个人简历

这是一个使用 Webpack 构建的现代化个人简历项目。

## 🚀 特性

- ✅ 使用 Webpack 5 构建
- ✅ 模块化代码结构
- ✅ 热更新开发服务器
- ✅ 生产环境优化
- ✅ 响应式设计
- ✅ 数据驱动，易于维护

## 📁 项目结构

```
简历/
├── src/
│   ├── index.js           # 入口文件
│   ├── index.html         # HTML模板
│   ├── styles/
│   │   └── main.css       # 样式文件
│   ├── data/
│   │   └── resumeData.js  # 简历数据
│   └── utils/
│       └── render.js      # 渲染函数
├── dist/                  # 构建输出目录
├── webpack.config.js      # Webpack配置
├── package.json           # 项目配置
└── README.md             # 项目说明
```

## 🛠️ 安装

```bash
npm install
```

## 📝 使用

### 开发模式

启动开发服务器，支持热更新：

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:8080`

### 生产构建

构建生产版本：

```bash
npm run build
```

构建后的文件会输出到 `dist/` 目录。

### 清理构建

清理 dist 目录：

```bash
npm run clean
```

## ✏️ 修改简历内容

所有简历数据都在 `src/data/resumeData.js` 文件中，修改这个文件即可更新简历内容。

### 修改基本信息

```javascript
basicInfo: {
    name: "你的名字",
    title: "你的职位",
    school: "你的学校",
    contacts: [...]
}
```

### 添加工作经历

```javascript
workExperience: [
    {
        company: "公司名称",
        position: "职位",
        date: "时间段",
        projects: [...]
    }
]
```

### 添加项目

```javascript
projects: [
    {
        name: "项目名称",
        description: "项目描述",
        challenges: "技术难点",
        highlights: "技术亮点",
        responsibility: "责任描述",
        tags: ["技术1", "技术2"]
    }
]
```

## 🎨 自定义样式

样式文件位于 `src/styles/main.css`，可以根据需要修改颜色、字体、布局等。

## 📦 部署

构建完成后，将 `dist/` 目录下的所有文件部署到任何静态网站托管服务即可，例如：

- GitHub Pages
- Netlify
- Vercel
- 阿里云 OSS
- 腾讯云 COS

## 📄 License

MIT